"""
Steps to make the watermark app:
1. make the GUI for uploading the file
1.a show a preview of the image before clicking "confirm" to actually process the photo
2. process the file and store it
3. show a preview of the file using GUI
4. Add buttons to have actions for the file: download or another photo

"""
from  tkinter_ui import App




app = App()
app.lift()


app.mainloop()

